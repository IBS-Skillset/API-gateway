---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: beta-app
  labels:
    app.kubernetes.io/name: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: api-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: api-gateway
    spec:
      containers:
        - name: api-gateway-api
          image: 306472407007.dkr.ecr.ap-south-1.amazonaws.com/ibs-skillset-happystays/api-gateway:31fee49
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 9192
          env:
            - name: HOST_UI
              value: mystaysui.beta-app
            - name: HOST_AUTH
              value: authserver.beta-app
            - name: HOST_REGISTRY
              value: service-registry.beta-app
